# Exclude Git metadata — not needed inside the Docker build context.
.git

# Exclude Air temporary files and local build artifacts.
tmp

# Exclude local development log files.
*.log

# Exclude editor-specific directories.
.vscode
